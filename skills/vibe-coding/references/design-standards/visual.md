# Visual（原则索引）

> **非施工入口。** 有 UI → 先 [LOAD-MAP.md](./LOAD-MAP.md)。  
> 具体册：tokens · pages · components · copy · anchor · audit · exemplars  

## 0. 先定表面与锚点

`surface` + `page_kind`/`motif` +（Build 前）`anchor`/`diverge` → 再按 LOAD-MAP 读册。强度见 LOAD-MAP 字段表。

## 1. 一屏一主任务

唯一主行动/主信息；次要降级；growth 首屏预算见 surfaces/consumer。

## 2. 先灰度，后上色

色角色与禁令 → [tokens/color-roles.md](./tokens/color-roles.md)。模板脸禁令真源 → [audit/ai-tells.md](./audit/ai-tells.md)。

## 3. 封闭尺度

→ [tokens/scale.md](./tokens/scale.md)。有宿主 DS → 用宿主。

## 4. 组件与页面

按 LOAD-MAP 读 pages + components。

## 5. 状态

空（first-run）/ 筛选零 / 载 / 错 / 成功 / 无权限 — 见 forms-states + copy。

## 6. 动效

→ [tokens/motion.md](./tokens/motion.md)。

## 7. AI 默认味

输出前扫 [audit/ai-tells.md](./audit/ai-tells.md)。

## 8. a11y

WCAG 子集 + [audit/web-interface.md](./audit/web-interface.md)。

## 9. 自检

- [ ] LOAD-MAP 已执行（或豁免适用）  
- [ ] surface / page_kind|motif /（Build）anchor  
- [ ] 尺度与色合规；overlays/copy 正确  
- [ ] 未平行第二套 DS  

## 10. Verify

视觉 alone 不关版失败，除非 Job 失败 / 严重度 4 / 违反已写入合同。  
发现分级映射：Nielsen **4 ↔ P0**，**3 ↔ P1**，**1–2 ↔ P2**（走查用语见 testing）。

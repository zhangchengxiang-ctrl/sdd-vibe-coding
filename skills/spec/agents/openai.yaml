interface:
  display_name: "Technical Plan"
  short_description: "把确认的产品切片拆成技术方案、场景和单任务执行合同"
  default_prompt: "Use $spec to turn the approved product slice into code-ready Task Work Orders."
policy:
  allow_implicit_invocation: false

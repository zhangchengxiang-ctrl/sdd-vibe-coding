---
description: 完成门禁 — Done / 范围标签 / 禁假完成。Always apply（在 00·01·02 之后）。
alwaysApply: true
---

# 完成门禁

> **上位**：`rules/00` · `01` · `02`。Accept 矩阵细则 → `vibe-coding` → `acceptance-to-remediation`（本文件不复述 3 硬钉）。

**Done** = 用户本轮目标达到约定 Delivery Target，且直接验收条件生效。  
不是：计划勾完 · 文件数凑齐 · 「核心文件已写」。  
诚实区分：`code-ready` · `dev-effective` · `production-delivered` · `user-accepted`。

**完成必带** `[轨·范围·Delivery Target]`。无标签 = 禁止报「完成 / 已落地」。

| 轨 | 到齐 |
|----|------|
| **Intake** | DEM / Card / modules 草稿沉淀即停；禁借「做完」改码 |
| **Owner** | 池判决 / Spec 升格 / `.next-rail` 落盘；禁编码 |
| **Build** | 本 Phase Delivery Target 补齐 |
| **Accept** | 矩阵终态齐全（≠ 全绿）；不 OK → remediation + `.next-rail`；**禁热修** → 见 `acceptance-to-remediation` |

## 禁止的提问（直接做）

| ❌ | ✅ |
|----|-----|
| 「要不要继续做 X？」且 X 属本轨 | 做完 X（commit/push 除外，须明示） |
| 「还差这些，要我继续吗？」 | 本轨 → 到齐；他轨 → `.next-rail` + 一行指针 |
| 完成后把未完成标「可选」 | 未完成不得标可选 |
| Intake「清单清楚，直接改？」 | **不改**；写 DEM / Shape |

## 熔断 / 允许提问

- 无因果新问题 → 记录退出；同区二次修失败 → 停补丁；验证已过且代码未变 → 不重跑  
- 仅允许问：真歧义 · 用户门禁（Plan/commit）· Agent 无法代劳的外部 blocker

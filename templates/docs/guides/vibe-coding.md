# Vibe Coding（宿主补充模板）

> **主流程在插件 skill `vibe-coding`。**  
> 本文只记**本仓特有**项（命令、URL、红线、账号）。禁止在此维护第二张活跃 Spec 表。

## 本仓开工

1. `AGENTS.md` — 命令与红线  
2. 先判轨（Intake / Owner / Build）  
3. Wish / 优化 → Intake → `docs/product/demand-pool.md`  
4. 点名 Spec → 只读该真源  

## 本仓红线（填写）

| 项 | 规则 |
|----|------|
| 浏览器验收 | （URL / 工具） |
| 部署 | （命令） |
| 产品回归 | （命令；合同见 `docs/product/foundation/product-regression.md`） |
| WIP | 与 `scripts/check-docs-sdd.sh` `WIP_CAP` 一致（默认 8） |

## 本仓验证命令

```bash
# 按 AGENTS.md 填写
```

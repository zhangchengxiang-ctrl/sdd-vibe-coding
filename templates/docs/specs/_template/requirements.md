# Requirements · <version-id>

## 用户 Job

作为 `<角色>`，在 `<情境>` 下，我要 `<完成的事>`，从而 `<价值>`。

## Requirements

| ID | 需求 | 优先级 | 证据级 | 验收条件（行为） | Scenario |
|---|---|---|---|---|---|
| R-001 | | P0 | Verified / Unverified | | SC-001 |

> `Unverified` 不得作为 P0 实施阻断或 Requirements Lock 条件；先补证据或降级。

## 非功能与约束

| ID | 约束 | 可观察判据 | Scenario / Test |
|---|---|---|---|
| NF-001 | | | |

## In / Out

- In：
- Out：

## 覆盖检查

- [ ] 每条 In-scope Requirement 至少映射一个 Scenario
- [ ] 每个 P0 需求含成功、关键失败或权限路径（行为 Oracle）
- [ ] 每个 P0 的证据级为 Verified，或已明确不进硬闸
- [ ] Out 未被静默实现

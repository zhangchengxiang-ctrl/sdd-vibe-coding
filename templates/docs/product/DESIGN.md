# DESIGN.md — 宿主设计上下文（可选）

> 由项目填写。插件 `design-standards` 为默认底线；本文件与 `AGENTS.md` / `PRODUCT.md` 覆盖品牌与锚点。  
> 路径建议：`<SDD docs root>/product/DESIGN.md`。

## 默认表面

- Default UI surface: product | consumer
- 默认 shell（仅 product）: unset | floating-card | flush-pane | doc-workspace | data-table
- 默认 consumer motif（仅 consumer）:
- 默认 visitor_mode: operate | persuade | read | experience
- 默认 knobs（variance/motion/density 1–10，可留空用插件默认）:

## 锚点

- 默认 anchor（真产品或 profile:*）:
- 反参考（不要像什么）:

## 品牌与 token

- 主色 accent（hex 或 token 名）:
- 字体（UI / Mono）:
- 圆角习惯:
- 组件库（antd / shadcn / 其他）与文档入口:
- DS for-agents / token 文档 URL（若有）:

## 文案

- 语气（正式 / 口语）:
- 禁用词:

## 备注

- 

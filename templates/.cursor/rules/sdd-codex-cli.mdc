---
description: Codex 派发只经 CLI；禁止 Codex MCP。真源 workflow-contract + dispatch-codex
alwaysApply: true
---

# Codex 派发硬门（CLI）

**真源：** 插件 `skills/dispatch-codex/SKILL.md` + `vibe-coding/references/workflow-contract.md`（指挥施工）。

**硬门（不可违反）：**

1. 派发只经 `codex-dispatch.sh` / `make codex-dispatch`（或包装缺失时的裸 `codex exec`+timeout）  
2. **禁止** Codex MCP：`user-codex` / `CallMcpTool`（`codex` / `codex-reply`）及依赖其的 Subagent  
3. 模型 `gpt-5.6-sol`；effort 仅 `medium`|`high`；`approval-policy=never`；一次一个完成单元  

本规则由 `sdd-vibe-coding` install 投影。

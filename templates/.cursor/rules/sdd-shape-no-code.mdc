---
description: 写码前须方案/Spec/Polish 授权。真源 workflow-contract (a)(b)(c)
alwaysApply: true
---

# 写码前硬闸

**真源：** 插件 `vibe-coding/references/workflow-contract.md`（写码授权）+ `project-kind.md`。

## 0. 项目类型门

先按 `sdd-vibe-entry` 解析 `project.kind`（`software` | `plugin` | `other`）。

- **`plugin`**：不适用本节；改 skills/templates/scripts。  
- **`other`**：放手，不建宿主式 `docs/product/`。  
- **禁止**把非 `software` 仓强行当成宿主编码项目。

## software 宿主

改业务代码前至少一项：已确认 Spec；或本轮明示「开始做 / 确认方案 / …」；或明示 Polish 且非 material。  
皆无 → 只写 product/ + 只读调查。下列**不算**写码授权：「设计方案」「走查」「优化体验」「Shape」。

本规则由 `sdd-vibe-coding` install 投影。

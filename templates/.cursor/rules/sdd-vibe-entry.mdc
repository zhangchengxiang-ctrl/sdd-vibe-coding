---
description: SDD 入口：先判 project.kind；software 须先 Read vibe-coding
alwaysApply: true
---

# SDD 入口硬门

**真源：** 插件 `vibe-coding/SKILL.md` + `references/project-kind.md` + `workflow-contract.md`。

## 0. 先判项目类型（`project.kind`）

**禁止默认假定**当前工作区是 coding / 软件交付仓。仅 `software` | `plugin` | `other`。

| kind | 入口 |
|---|---|
| `plugin` | 读 `ARCHITECTURE.md`；改 skills/templates/scripts |
| `software` | 下方 |
| `other` | 放手 |

## software 宿主

命中产品/编码/验收/发布意图时，**第一个工具调用**必须是 Read 已安装 `vibe-coding` 的 `SKILL.md`，再读宿主 `AGENTS.md`。  
未完成 → **禁止**改业务代码、禁止宣称可交付、禁止生产部署。

本规则由 `sdd-vibe-coding` install 投影。
